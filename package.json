{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "dependencies": {},
  "devDependencies": {
    "giochart": "^1.5.10",
    "lerna": "^3.16.4"
  },
  "scripts": {
    "doc:start": "yarn workspace docs start",
    "test": "yarn test:components && yarn test:utils",
    "test:components": "yarn workspace @gio-core/components test",
    "test:utils": "yarn workspace @gio-core/utils test"
  }
}

test('', () => {})
/*
import React from 'react';
import { shallow } from 'enzyme';
import toJSON from 'enzyme-to-json';
import SelectGroup from '../SelectGroup';

const options = [
  { groupId: 'events', groupName: '选择事件', id: 'a', name: 'a' },
  { groupId: 'events', groupName: '选择事件', id: 'b', name: 'b' },
  { groupId: 'events', groupName: '选择事件', id: 'c', name: 'c' },
]

describe('SelectGroup unit test', () => {
  const Component = (
    <SelectGroup
      value={[]}
      onChange={jest.fn}
      options={options}
      valueKey={'id'}
      isMultiple={true}
      onSelect={jest.fn}
      handleChange={jest.fn}
      height={450}
    />
  )
  it('renders correctly', () => {
    const wrapper = shallow(Component);
    expect(toJSON(wrapper)).toMatchSnapshot();
  });
});

*/